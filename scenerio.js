//change this to be 

function runscenerio1() {

	if (worldVar.scenerioHasBeenLaunched === 0) {


		addNewLifeComplete(worldVar.totalLifeForms, 500, 300, "gray", "No", "No");
		addNewLifeComplete(worldVar.totalLifeForms, 100, 200, "gray", "Yellow", "No");
		addNewLifeComplete(worldVar.totalLifeForms, 300, 400, "gray", "No", "Blue");

	}

}